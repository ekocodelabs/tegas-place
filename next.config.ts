import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // Disable development indicators for a cleaner UI
};

export default nextConfig;
